<% content_for :top_content do %>

  <% if user_signed_in? %>
    <% if current_user.user_type? %>
    <div class="jumbotron">
      <div class="container">
        <h1>Welcome to Quizapp </h1>
        <p>You Are Adminstrator</p> 
        <%= link_to 'Admin Page',rails_admin_path, class: 'btn btn-primary btn-lg'%>
      </div>
    </div>
    <% else %>
      <div class="jumbotron">
    <div class="container">
      <h1>Welcome to Quizapp </h1>
      <p>Select Your Quiz here:</p>
    </div>
    </div>
    <% end %>
   
  <div class="container table-responsive">
  <h2> Sports Quiz </h2>
  <table class="table table-hover">
  <tr class="active">
    <th>
      Sub Genre
    </th>
    <th>
      Current Score
    </th>
    <th>
      Your Highest
    </th>
    <th>
      Options
    </th>
  </tr>
  <tr>
   <td>Cricket </td>
   <td><%= $sc1%></td>
   <td><%= $hsc1 %></td>
   <td>
       <% if $st1 <= 0 %>
         <%= link_to 'Resume', "/questions/1", class: 'btn btn-primary disabled' %>
       <% else %>   
         <%= link_to 'Resume', "/questions/1", class: 'btn btn-primary' %>
        <% end %>  
       <%= link_to 'Restart', "/questions/1/reset", class: 'btn btn-primary' %>
       <%= link_to 'Leaderboard', "/leaderboard/1", class: 'btn btn-primary'%></td>
   </tr>
   <tr>
   <td>Football </td>
   <td><%= $sc2%></td>
   <td><%= $hsc2 %></td>
   <td><% if $st2 <= 0 %>
         <%= link_to 'Resume', "/questions/2", class: 'btn btn-primary disabled' %>
       <% else %>   
         <%= link_to 'Resume', "/questions/2", class: 'btn btn-primary' %>
        <% end %>  
       <%= link_to 'Restart', "/questions/2/reset", class: 'btn btn-primary' %>
       <%= link_to 'Leaderboard', "/leaderboard/2", class: 'btn btn-primary'%></td>
   </tr>      

  </table>
  </div>  

  <div class="container table-responsive">
  <h2> Movies Quiz </h2>
  <table class="table table-hover">
  <tr class="active">
    <th>
      Sub Genre
    </th>
    <th>
      Current Score
    </th>
    <th>
      Your Highest
    </th>
    <th>
      Options
    </th>
  </tr>
  <tr>
   <td>Bollywood </td>
   <td><%= $sc3%></td>
   <td><%= $hsc3 %></td>
   <td><% if $st3 <= 0 %>
         <%= link_to 'Resume', "/questions/3", class: 'btn btn-primary disabled' %>
       <% else %>   
         <%= link_to 'Resume', "/questions/3", class: 'btn btn-primary' %> 
        <% end %> 
       <%= link_to 'Restart', "/questions/3/reset", class: 'btn btn-primary' %>
       <%= link_to 'Leaderboard', "/leaderboard/3", class: 'btn btn-primary'%></td>
   </tr>
   <tr>
   <td>Hollywood </td>
   <td><%= $sc4%></td>
   <td><%= $hsc4 %></td>
   <td><% if $st4 <= 0 %>
         <%= link_to 'Resume', "/questions/4", class: 'btn btn-primary disabled' %>
       <% else %>   
         <%= link_to 'Resume', "/questions/4", class: 'btn btn-primary' %>
        <% end %>  
       <%= link_to 'Restart', "/questions/4/reset", class: 'btn btn-primary' %>
       <%= link_to 'Leaderboard', "/leaderboard/4", class: 'btn btn-primary'%></td>
   </tr>      

  </table>
  </div>  



  <% else %>	
  <div class="jumbotron">
    <div class="container">  
      <h1>Welcome!</h1>
      <p>Register to get started.</p>
      <p>
        <%= link_to 'Register', new_user_registration_path, class: 'btn btn-primary btn-lg' %>
      </p>
  </div>
    </div>
  <% end %>

<% end %>
  